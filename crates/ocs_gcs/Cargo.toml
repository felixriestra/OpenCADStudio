[package]
name = "ocs_gcs"
version = "0.1.0"
edition = "2021"
description = "2D geometric constraint solver for Mac2CAM sketches, ported from FreeCAD's planegcs."
license = "LGPL-2.1-or-later"

[dependencies]
nalgebra = "0.33"

[lib]
name = "ocs_gcs"
path = "src/lib.rs"
